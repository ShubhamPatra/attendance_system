# ============================================================
# AUTOATTENDANCE - DOCKER DEPENDENCIES
# Docker-specific requirements (excludes torch for flexible installation)
# Used by docker/Dockerfile* for optimized builds
# Python 3.9+ | Last updated: 2026-04-16
# ============================================================

# Web Framework and extensions
Flask>=3.0,<4.0
Flask-SocketIO>=5.3,<6.0
Flask-RESTX>=1.2,<2.0
Flask-WTF>=1.1,<2.0
Werkzeug>=3.0,<4.0
Jinja2>=3.0,<4.0
eventlet>=0.33,<1.0
python-socketio>=5.10,<6.0
python-engineio>=4.10,<5.0

# ML / Computer Vision
opencv-contrib-python>=4.8,<5.0
insightface>=0.7,<0.8
numpy>=1.24,<2.0
scipy>=1.10,<2.0
Pillow>=10.0,<11.0

# ONNX Runtime - CPU variant (will be overridden by docker builds if GPU needed)
onnxruntime>=1.17,<2.0

# NOTE: torch and torchvision are installed separately in Dockerfile
# to support CPU/GPU variants via PyTorch index URL

# Database
pymongo>=4.5,<5.0
dnspython>=2.4,<3.0

# DevOps / utilities
gunicorn>=21.0,<24.0
python-dotenv>=1.0,<2.0

# Miscellaneous
bcrypt>=4.1,<5.0
email-validator>=2.0,<3.0
Markdown>=3.5,<4.0
apscheduler>=3.10,<4.0
