# ============================================================
# AUTOATTENDANCE - DEPENDENCY MANIFEST
# Python 3.9+ | Version Independent
# ============================================================
#
# This file includes base dependencies + CPU runtime (onnxruntime)
# For development: pip install -e ".[dev]"
# For GPU: pip install -e ".[gpu]"
# 
# Or use modular requirements files directly:
#   - requirements/base.txt       (shared core)
#   - requirements/cpu.txt        (CPU onnxruntime - default)
#   - requirements/gpu.txt        (GPU onnxruntime - for NVIDIA GPUs)
#   - requirements/dev.txt        (testing & development)
#   - requirements-docker.txt     (Docker builds)
#
# Version Compatibility:
#   - Python 3.9, 3.10, 3.11, 3.12, 3.13
#   - Linux, macOS, Windows
#   - CPU and GPU (CUDA 11.8+, CUDA 12.x)
# ============================================================

# Include base dependencies with version ranges (Python 3.9+)
-r requirements/base.txt

# Include CPU runtime for ONNX (default)
# If using GPU, uninstall onnxruntime and install onnxruntime-gpu instead
-r requirements/cpu.txt
